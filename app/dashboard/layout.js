export const metadata = {
  title: 'Dashboard - LedgerLite',
  description: 'Your business dashboard with metrics, quick actions, and recent activity',
};

export default function DashboardLayout({ children }) {
  return children;
} 